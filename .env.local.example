# Spotify PKCE auth — no client secret needed (browser-only flow)
NEXT_PUBLIC_SPOTIFY_CLIENT_ID=your_spotify_client_id_here

# Must use 127.0.0.1, NOT localhost (Spotify no longer accepts localhost as of April 2025)
NEXT_PUBLIC_REDIRECT_URI=http://127.0.0.1:3000/api/spotify/callback
